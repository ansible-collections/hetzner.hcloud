bugfixes:
  - load_balancer_service - Fix ``health_check`` fields not explicitly set by the user being sent to the API as ``null``, causing the request to fail with ``invalid input in field 'health_check'``.
